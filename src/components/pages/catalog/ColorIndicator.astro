---
import { cn } from "../../../utils/cn";
interface Props {
  color: string;
}

const{color} = Astro.props;
---

<div class={cn("rounded-full w-6 h-6", color === "white" ? "bg-white" : "bg-[#979797]")} />
