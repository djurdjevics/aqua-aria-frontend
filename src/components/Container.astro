---
import { cn } from "../utils/cn";

interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<section class={cn("max-w-7xl mx-auto px-4", className)}>
  <slot />
</section>;

